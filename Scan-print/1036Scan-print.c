#include <stdio.h>

int main() {
    char a;
    scanf("%c", &a);
    printf("%d", a);
    return 0;
}

//ASCII (American Standard Code for Information Interchange) table is a standard code which is speciallised for letters and signs
//ASCII code is used to convert letters and signs to binary digits to save in the memory
